# 接入层骨架依赖(本期 PR)
requests>=2.31
websocket-client>=1.7
python-dotenv>=1.0

# 发音评测(Azure)。无 key 时不会被调用,但导入需要它;
# 若安装环境受限,可延后安装——service.py 对缺失做了优雅降级。
azure-cognitiveservices-speech>=1.37

# 百炼(DashScope)ASR —— 浏览器 Web Speech 的服务端兜底
dashscope>=1.20

# 测试
pytest>=8.0
pytest-cov>=5.0

# Web 层(Flask 内置服务器 threaded 模式;SSE 流式)
flask>=3.0
